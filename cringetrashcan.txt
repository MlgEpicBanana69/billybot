#@billybot.command(aliases=["MoveTo", "Moveto"])
#async def moveto(ctx, *, v_channel):
#    """Moves billybot to a given voice channel."""
#    for voice_channel in ctx.guild.channels[1].channels:
#        if voice_channel.name ==   v_channel:
#            voice_client = ctx.guild.voice_client
#            if voice_client is not None:
#                await voice_client.main_ws.voice_state(ctx.guild.id, voice_client.channel.id, self_mute=False, self_deaf=True)
#                return
#            else:
#                await ctx.message.channel.send
#
#    await ctx.message.channel.send(ctx.author.mention + " {0} is not a valid voice channel!")
